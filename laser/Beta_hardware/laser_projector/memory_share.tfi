


// Instantiate the module
memory_share instance_name (
    .clk(clk), 
    .mwe_host(mwe_host), 
    .mwe_client(mwe_client), 
    .host_addr(host_addr), 
    .client_addr(client_addr), 
    .host_din(host_din), 
    .client_din(client_din), 
    .host_dout(host_dout), 
    .client_dout(client_dout)
    );


