


// Instantiate the module
wb_i2c_controller instance_name (
    .clk(clk), 
    .reset(reset), 
    .din(din), 
    .addr(addr), 
    .start(start), 
    .wren(wren), 
    .dout(dout), 
    .done(done), 
    .scl(scl), 
    .sda(sda)
    );


